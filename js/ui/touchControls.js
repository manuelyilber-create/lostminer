// js/ui/touchControls.js
export class TouchControls {
    constructor(canvas, keys) {
        this.canvas = canvas;
        this.keys = keys;
    }

    render(ctx) {
        // Renderizador base de controles táctiles
    }
}
